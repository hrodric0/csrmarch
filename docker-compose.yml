# =============================================================================
# docker-compose.yml — CSMR local development stack
#
# Brings up the full composition locally without Kubernetes.
# Useful for rapid iteration on the PoC before deploying to Minikube.
#
# Architecture (Docker Compose specific):
# - All services share the same Docker network
# - Sidecars communicate with apps via 127.0.0.1 (localhost)
# - Proxy routes to sidecars on port 9000
# - Sidecars communicate with each other via service names
#
# Usage:
#   docker compose up --build
#   curl -X POST http://localhost:8080/api/command \
#     -H 'Content-Type: application/json' \
#     -d '{"id":1,"method":"put","params":{"key":"hello","value":"world"}}'
# =============================================================================

services:

  # ── ZooKeeper ────────────────────────────────────────────────────────────────
  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "2181"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── ZooKeeper ring-membership initializer (one-shot) ─────────────────────────
  # In Kubernetes the control-plane operator provisions /csmr/rings/* via
  # ZooKeeperRingProvisioner. Docker Compose has no control plane, so this one-shot
  # job seeds the CSMR membership layer that each sidecar's ZooKeeperRingDiscovery
  # reads at startup (path /csmr/rings/<ringId>/members/<nodeId>, data = sidecar
  # address). Without it sidecars discover 0 members and start degenerate 1-node
  # rings. Idempotent (tolerates NodeExists) so it re-seeds on every `up`, including
  # after a ZooKeeper restart wipes the (non-persistent) tree. All sidecars
  # depend_on this via service_completed_successfully.
  zk-init:
    image: confluentinc/cp-zookeeper:7.6.0
    depends_on:
      zookeeper:
        condition: service_healthy
    restart: "no"
    entrypoint: ["bash", "-c"]
    ports: []
    command:
      - |
        set -e
        ZK=zookeeper:2181
        create() {
          # create <path> [data]; ignore "already exists"
          zookeeper-shell "$$ZK" create "$$1" "$${2:-}" 2>&1 | grep -v 'NodeExists' || true
        }
        echo "Provisioning /csmr/rings membership in ZooKeeper at $$ZK ..."
        create /csmr ''
        create /csmr/rings ''
        create /csmr/rings/kv_store_Put ''
        create /csmr/rings/kv_store_Put/members ''
        create /csmr/rings/kv_store_Put/members/0 'sidecar-kvs-0:9000'
        create /csmr/rings/kv_store_Put/members/1 'sidecar-kvs-1:9000'
        create /csmr/rings/kv_store_Put/members/2 'sidecar-kvs-2:9000'
        create /csmr/rings/logger_Append ''
        create /csmr/rings/logger_Append/members ''
        create /csmr/rings/logger_Append/members/0 'sidecar-log-0:9000'
        create /csmr/rings/logger_Append/members/1 'sidecar-log-1:9000'
        create /csmr/rings/logger_Append/members/2 'sidecar-log-2:9000'
        echo "ZooKeeper ring membership provisioned."
        # --- URingPaxos acceptor-watch race fix (cold start) ---
        # URingPaxos RingManager.init()->getConfig() reads /ringpaxos/topology<N>/acceptors
        # with a one-shot watch (RingManager.java:111) BEFORE this node registers its own
        # acceptor znode (Node.java:129 init runs before :143 registerRole). On a cold start
        # the /acceptors parent does not exist yet, so getChildren throws NoNodeException
        # (caught/ignored at RingManager.java:120): NO watch is armed and `last_acceptor`
        # stays frozen at 0. process()'s acceptor branch then never fires, so Phase1/Phase1Range
        # tokens are never re-addressed to the coordinator (the last_acceptor==self check fails)
        # and return to the coordinator with quorum 0 -> "ring end without quorum (0)" ->
        # "Coordinator timeout in phase1 reservation" -> proxy "Quorum not reached".
        # Pre-creating the PERSISTENT parent makes getChildren return [] and ARM the watch, so
        # subsequent NodeChildrenChanged events recompute last_acceptor=max=2 on every node.
        # URingPaxos creates this same parent lazily as PERSISTENT guarded by an exists() check
        # (TopologyManager.java:486-488), so pre-creating it is idempotent and skips that create
        # (no NodeExistsException). topology1=kv_store_Put, topology2=logger_Append.
        echo "Pre-creating URingPaxos /acceptors parents to arm the election watch ..."
        create /ringpaxos ''
        create /ringpaxos/topology1 ''
        create /ringpaxos/topology1/acceptors ''
        create /ringpaxos/topology2 ''
        create /ringpaxos/topology2/acceptors ''
        echo "URingPaxos acceptor parents provisioned."

        # --- Dissertation Scenario 3 (Argument Partition) rings ---
        # kv_store_ring1 -> topology3, kv_store_ring2 -> topology4.
        # Each is a fresh 3-node KVS ring so partitioned_put routes keys in [a-m]
        # to ring1 and [n-z] to ring2 (see full-csmr-composition.yaml PartitionedKVS).
        create /csmr/rings/kv_store_ring1 ''
        create /csmr/rings/kv_store_ring1/members ''
        create /csmr/rings/kv_store_ring1/members/0 'sidecar-kvs-ring1-0:9000'
        create /csmr/rings/kv_store_ring1/members/1 'sidecar-kvs-ring1-1:9000'
        create /csmr/rings/kv_store_ring1/members/2 'sidecar-kvs-ring1-2:9000'
        create /csmr/rings/kv_store_ring2 ''
        create /csmr/rings/kv_store_ring2/members ''
        create /csmr/rings/kv_store_ring2/members/0 'sidecar-kvs-ring2-0:9000'
        create /csmr/rings/kv_store_ring2/members/1 'sidecar-kvs-ring2-1:9000'
        create /csmr/rings/kv_store_ring2/members/2 'sidecar-kvs-ring2-2:9000'
        create /csmr/rings/prng_service ''
        create /csmr/rings/prng_service/members ''
        create /csmr/rings/prng_service/members/0 'sidecar-prng-0:9000'
        create /csmr/rings/prng_service/members/1 'sidecar-prng-1:9000'
        create /csmr/rings/prng_service/members/2 'sidecar-prng-2:9000'
        create /csmr/rings/counter_service ''
        create /csmr/rings/counter_service/members ''
        create /csmr/rings/counter_service/members/0 'sidecar-counter-0:9000'
        create /csmr/rings/counter_service/members/1 'sidecar-counter-1:9000'
        create /csmr/rings/counter_service/members/2 'sidecar-counter-2:9000'
        echo "Dissertation ring membership (partition rings + chaining services) provisioned."

        create /ringpaxos/topology3 ''
        create /ringpaxos/topology3/acceptors ''
        create /ringpaxos/topology4 ''
        create /ringpaxos/topology4/acceptors ''
        create /ringpaxos/topology5 ''
        create /ringpaxos/topology5/acceptors ''
        create /ringpaxos/topology6 ''
        create /ringpaxos/topology6/acceptors ''
        echo "URingPaxos acceptor parents (topology3-6) provisioned."

  # ── CSMR Control Plane (Operator) ────────────────────────────────────────────
  # Note: Control plane requires Kubernetes API. Disabled in Docker Compose mode.
  # Uncomment and configure kubeconfig if running with Minikube.
  # control-plane:
  #   build:
  #     context: .
  #     dockerfile: docker/Dockerfile.control-plane
  #   environment:
  #     ZOOKEEPER_CONNECT: "zookeeper:2181"
  #   volumes:
  #     - ~/.kube/config:/home/csmr/.kube/config  # Mount kubeconfig for Minikube
  #   depends_on:
  #     zookeeper:
  #       condition: service_healthy

  # ── CSMR Proxy ───────────────────────────────────────────────────────────────
  proxy:
    build:
      context: .
      dockerfile: docker/Dockerfile.proxy
    ports:
      - "8080:8080"
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      # Empty (default) => proxy loads the bundled classpath composition. The E2E
      # test scripts override this env when bringing up the stack so the proxy can
      # load a specific bundled YAML without a host bind-mount:
      #   - Scenario 3/4: /config/composition/full-csmr-composition.yaml
      #   - Chaining PoC: /config/composition/chaining-smr-composition.yaml
      # (Both are baked into the proxy image by docker/Dockerfile.proxy.)
      CSMR_ROUTING_YAML_PATH: "${CSMR_ROUTING_YAML_PATH:-}"
    depends_on:
      zookeeper:
        condition: service_healthy
      sidecar-kvs-0:
        condition: service_healthy
      sidecar-kvs-1:
        condition: service_healthy
      sidecar-kvs-2:
        condition: service_healthy
      sidecar-log-0:
        condition: service_healthy
      sidecar-log-1:
        condition: service_healthy
      sidecar-log-2:
        condition: service_healthy
      # Scenario 3 (Argument Partition) rings — so the proxy only comes up once
      # partitioned_put can reach both kv_store_ring1 and kv_store_ring2.
      sidecar-kvs-ring1-0:
        condition: service_healthy
      sidecar-kvs-ring1-1:
        condition: service_healthy
      sidecar-kvs-ring1-2:
        condition: service_healthy
      sidecar-kvs-ring2-0:
        condition: service_healthy
      sidecar-kvs-ring2-1:
        condition: service_healthy
      sidecar-kvs-ring2-2:
        condition: service_healthy
      # PoC2 Chaining SMRs (init_counter chains prng_service -> counter_service).
      sidecar-prng-0:
        condition: service_healthy
      sidecar-prng-1:
        condition: service_healthy
      sidecar-prng-2:
        condition: service_healthy
      sidecar-counter-0:
        condition: service_healthy
      sidecar-counter-1:
        condition: service_healthy
      sidecar-counter-2:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── KVS Ring: 3 replicas (app + sidecar each) ─────────────────────────────────
  kvs-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_Put"
      RING_ID_NUMERIC: "1"
      RING_SIZE: "3"
      NODE_ID: "0"
      APP_PORT: "8081"
      APP_HOST: "kvs-0"  # Docker Compose service name for app
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19000:9000"  # Expose sidecar on host port 19000
    expose:
      - "9000"
    # Node 0 registers its acceptor znode FIRST. Coordinator election is driven
    # in-sidecar: PaxosRingNode.startCoordinatorRoleIfElected() polls
    # RingManager.getCoordinatorID() on node 0 and, once node 0 is the elected
    # coordinator id, starts `new CoordinatorRole(rm)` directly. Because node 0
    # registers before any follower, no follower ever transiently sees itself as
    # min(acceptors), so URingPaxos's own notifyNewCoordinator() never spuriously
    # starts a competing CoordinatorRole on node 1/2 — there is exactly one
    # coordinator (node 0). The followers depend_on this service so it wins the race.
    depends_on:
      kvs-0:
        condition: service_started
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  kvs-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_Put"
      RING_ID_NUMERIC: "1"
      RING_SIZE: "3"
      NODE_ID: "1"
      APP_PORT: "8081"
      APP_HOST: "kvs-1"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19001:9000"
    expose:
      - "9000"
    # Follower: registers its acceptor znode AFTER node 0 (depends_on sidecar-kvs-0)
    # so it never transiently becomes min(acceptors) and never starts a competing
    # CoordinatorRole — see the note on sidecar-kvs-0.
    depends_on:
      kvs-1:
        condition: service_started
      sidecar-kvs-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  kvs-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_Put"
      RING_ID_NUMERIC: "1"
      RING_SIZE: "3"
      NODE_ID: "2"
      APP_PORT: "8081"
      APP_HOST: "kvs-2"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19002:9000"
    expose:
      - "9000"
    # Follower: registers AFTER node 0 (depends_on sidecar-kvs-0) — see sidecar-kvs-0.
    depends_on:
      kvs-2:
        condition: service_started
      sidecar-kvs-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  # ── Log Ring: 3 replicas (app + sidecar each) ──────────────────────────────────
  log-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-log
    environment:
      SERVER_PORT: "8082"
    expose:
      - "8082"

  sidecar-log-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "logger_Append"
      RING_ID_NUMERIC: "2"
      RING_SIZE: "3"
      NODE_ID: "0"
      APP_PORT: "8082"
      APP_HOST: "log-0"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19010:9000"
    expose:
      - "9000"
    # Node 0 registers its acceptor znode FIRST; coordinator election is driven
    # in-sidecar (single coordinator) — see the detailed note on sidecar-kvs-0.
    depends_on:
      log-0:
        condition: service_started
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  log-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-log
    environment:
      SERVER_PORT: "8082"
    expose:
      - "8082"

  sidecar-log-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "logger_Append"
      RING_ID_NUMERIC: "2"
      RING_SIZE: "3"
      NODE_ID: "1"
      APP_PORT: "8082"
      APP_HOST: "log-1"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19011:9000"
    expose:
      - "9000"
    # Follower: registers AFTER node 0 (depends_on sidecar-log-0) — see sidecar-kvs-0.
    depends_on:
      log-1:
        condition: service_started
      sidecar-log-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  log-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-log
    environment:
      SERVER_PORT: "8082"
    expose:
      - "8082"

  sidecar-log-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "logger_Append"
      RING_ID_NUMERIC: "2"
      RING_SIZE: "3"
      NODE_ID: "2"
      APP_PORT: "8082"
      APP_HOST: "log-2"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19012:9000"
    expose:
      - "9000"
    # Follower: registers AFTER node 0 (depends_on sidecar-log-0) — see sidecar-kvs-0.
    depends_on:
      log-2:
        condition: service_started
      sidecar-log-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  # ── Scenario 3 (Argument Partition) Ring 1: kv_store_ring1 (topology3) ────────
  kvs-ring1-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-ring1-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_ring1"
      RING_ID_NUMERIC: "3"
      RING_SIZE: "3"
      NODE_ID: "0"
      APP_PORT: "8081"
      APP_HOST: "kvs-ring1-0"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19020:9000"
    expose:
      - "9000"
    depends_on:
      kvs-ring1-0:
        condition: service_started
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  kvs-ring1-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-ring1-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_ring1"
      RING_ID_NUMERIC: "3"
      RING_SIZE: "3"
      NODE_ID: "1"
      APP_PORT: "8081"
      APP_HOST: "kvs-ring1-1"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19021:9000"
    expose:
      - "9000"
    depends_on:
      kvs-ring1-1:
        condition: service_started
      sidecar-kvs-ring1-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  kvs-ring1-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-ring1-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_ring1"
      RING_ID_NUMERIC: "3"
      RING_SIZE: "3"
      NODE_ID: "2"
      APP_PORT: "8081"
      APP_HOST: "kvs-ring1-2"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19022:9000"
    expose:
      - "9000"
    depends_on:
      kvs-ring1-2:
        condition: service_started
      sidecar-kvs-ring1-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  # ── Scenario 3 (Argument Partition) Ring 2: kv_store_ring2 (topology4) ────────
  kvs-ring2-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-ring2-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_ring2"
      RING_ID_NUMERIC: "4"
      RING_SIZE: "3"
      NODE_ID: "0"
      APP_PORT: "8081"
      APP_HOST: "kvs-ring2-0"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19030:9000"
    expose:
      - "9000"
    depends_on:
      kvs-ring2-0:
        condition: service_started
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  kvs-ring2-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-ring2-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_ring2"
      RING_ID_NUMERIC: "4"
      RING_SIZE: "3"
      NODE_ID: "1"
      APP_PORT: "8081"
      APP_HOST: "kvs-ring2-1"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19031:9000"
    expose:
      - "9000"
    depends_on:
      kvs-ring2-1:
        condition: service_started
      sidecar-kvs-ring2-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  kvs-ring2-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-kvs
    environment:
      SERVER_PORT: "8081"
    expose:
      - "8081"

  sidecar-kvs-ring2-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "kv_store_ring2"
      RING_ID_NUMERIC: "4"
      RING_SIZE: "3"
      NODE_ID: "2"
      APP_PORT: "8081"
      APP_HOST: "kvs-ring2-2"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19032:9000"
    expose:
      - "9000"
    depends_on:
      kvs-ring2-2:
        condition: service_started
      sidecar-kvs-ring2-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  # ── PoC2 Chaining SMRs: PRNG service (topology5) ─────────────────────────────
  prng-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-prng
    environment:
      SERVER_PORT: "8086"
    expose:
      - "8086"

  sidecar-prng-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "prng_service"
      RING_ID_NUMERIC: "5"
      RING_SIZE: "3"
      NODE_ID: "0"
      APP_PORT: "8086"
      APP_HOST: "prng-0"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19040:9000"
    expose:
      - "9000"
    depends_on:
      prng-0:
        condition: service_started
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  prng-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-prng
    environment:
      SERVER_PORT: "8086"
    expose:
      - "8086"

  sidecar-prng-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "prng_service"
      RING_ID_NUMERIC: "5"
      RING_SIZE: "3"
      NODE_ID: "1"
      APP_PORT: "8086"
      APP_HOST: "prng-1"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19041:9000"
    expose:
      - "9000"
    depends_on:
      prng-1:
        condition: service_started
      sidecar-prng-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  prng-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-prng
    environment:
      SERVER_PORT: "8086"
    expose:
      - "8086"

  sidecar-prng-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "prng_service"
      RING_ID_NUMERIC: "5"
      RING_SIZE: "3"
      NODE_ID: "2"
      APP_PORT: "8086"
      APP_HOST: "prng-2"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19042:9000"
    expose:
      - "9000"
    depends_on:
      prng-2:
        condition: service_started
      sidecar-prng-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  # ── PoC2 Chaining SMRs: Counter service (topology6) ──────────────────────────
  counter-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-counter
    environment:
      SERVER_PORT: "8087"
    expose:
      - "8087"

  sidecar-counter-0:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "counter_service"
      RING_ID_NUMERIC: "6"
      RING_SIZE: "3"
      NODE_ID: "0"
      APP_PORT: "8087"
      APP_HOST: "counter-0"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19050:9000"
    expose:
      - "9000"
    depends_on:
      counter-0:
        condition: service_started
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  counter-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-counter
    environment:
      SERVER_PORT: "8087"
    expose:
      - "8087"

  sidecar-counter-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "counter_service"
      RING_ID_NUMERIC: "6"
      RING_SIZE: "3"
      NODE_ID: "1"
      APP_PORT: "8087"
      APP_HOST: "counter-1"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19051:9000"
    expose:
      - "9000"
    depends_on:
      counter-1:
        condition: service_started
      sidecar-counter-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s

  counter-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.app-counter
    environment:
      SERVER_PORT: "8087"
    expose:
      - "8087"

  sidecar-counter-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.sidecar-paxos
    environment:
      ZOOKEEPER_CONNECT: "zookeeper:2181"
      RING_ID: "counter_service"
      RING_ID_NUMERIC: "6"
      RING_SIZE: "3"
      NODE_ID: "2"
      APP_PORT: "8087"
      APP_HOST: "counter-2"
      STABLE_STORAGE_TYPE: "InMemory"
      JAVA_TOOL_OPTIONS: "-XX:MaxDirectMemorySize=256m -Xmx512m -Dpaxos.learner.auto_trim=true -Dpaxos.learner.buffer_size=1000"
    ports:
      - "19052:9000"
    expose:
      - "9000"
    depends_on:
      counter-2:
        condition: service_started
      sidecar-counter-0:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
      zk-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9000/internal/health"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s